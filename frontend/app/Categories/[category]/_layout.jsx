/* import { Stack } from "expo-router";

export default function CategoryDetailLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: true, // ✅ now this page has its own isolated header
      }}
    />
  );
} */
